## 修复

- 修复 GitHub Actions 已完成却仍显示“运行中”的问题。流水线状态现在独立于仓库元数据刷新，每轮检查当前范围内最多 8 个仓库，并优先检查运行中的任务。
- 同步成功／失败结果、执行耗时、详情页面和菜单栏 RUN／FAIL 数量；仓库未更新时也能发现重新运行。
- 修复详情请求晚返回时，将流水线写回旧状态的问题；合并作业详情时保留当前整体状态和已读取的日志。
- 保持实例独立刷新、3 秒超时以及离线缓存保留。
- 菜单栏 RUN／FAIL 字体调整为 7，保持单行垂直居中和双行排列。
- 每次打开面板时立即清除 FAIL 未读计数，无需切换标签页；RUN 数量和流水线失败记录保留。

## 验证

- 57 项测试通过，包括无仓库变更时的成功完成、失败完成、重新运行，以及晚到详情不覆盖完成状态。
- 已通过 GitHub API 确认先前发布流水线成功完成，并确认仓库更新时间早于流水线完成时间。

## 更新

- 已安装支持 Sparkle 的版本，可使用右上角蓝色下载按钮更新并重启。
- 手动安装：下载 `StackHub-1.0.6-macos-arm64.zip`，解压后将 `StackHub.app` 放入“应用程序”。适用于 Apple Silicon 和 macOS 14 及以上。
- 更新重启会停止 StackHub 管理的本地服务，重新打开后需按需启动服务。
