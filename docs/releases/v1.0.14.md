## 更新内容

- 修复 GitLab 等待、手动、跳过和取消等状态被误显示为失败的问题，并持续刷新尚未结束的流水线。
- 按流水线实际阶段顺序显示作业，避免作业重试或接口返回顺序导致阶段错位。
- 每个子任务独立显示状态圆点，鼠标悬浮可查看对应的作业名称和状态。
- 流水线卡片显示实际运行的分支或标签，并自动刷新旧缓存中误判的作业状态。

## What's new

- Correct GitLab status mapping so waiting, manual, skipped, and canceled jobs are no longer shown as failures. Keep polling pipelines that can still change status.
- Display jobs in the pipeline's declared stage order, including when jobs have been retried.
- Show a separate status dot for every job, with its name and status available on hover.
- Show the actual pipeline branch or tag on activity cards and refresh incorrectly classified job states in older caches.

---

macOS 14+ · Apple Silicon
